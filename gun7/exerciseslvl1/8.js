//Circumference of a circle is calculated as follows: circumference = 2πr. Write a function which calculates circumOfCircle
function circumOfCircle(r, PI = Math.PI) {
    let circumference = 2 * PI * r;
    return circumference;
}